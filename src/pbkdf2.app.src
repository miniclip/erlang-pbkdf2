{application, pbkdf2, [
    {description, "Erlang PBKDF2 Key Derivation Function"},
    {vsn, git},
    {registered, []},

    {applications, [
        % OTP dependencies
        crypto,
        kernel,
        stdlib
        % External dependencies
    ]},

    {env, []}
]}.
